import { WetterBeschreibungPipe } from './wetter-beschreibung.pipe';

describe('WetterBeschreibungPipe', () => {
  it('create an instance', () => {
    const pipe = new WetterBeschreibungPipe();
    expect(pipe).toBeTruthy();
  });
});
